<?xml version="1.0" encoding="utf-8"?>
<!--
  Liquid Glass 风格选择弹窗。
  注意：Dialog 是独立窗口，玻璃无法自动捕获宿主窗口内容，
  必须在代码里把 backdropSource 指向 Activity 的 content view。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="20dp"
    android:paddingEnd="20dp">

    <com.example.liquidglass.LiquidGlassView
        android:id="@+id/glass_picker"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:bevelWidth="28dp"
        app:blurAmount="0.35"
        app:cornerRadius="28dp"
        app:dispersionStrength="0.32"
        app:glassMaterial="regular"
        app:refractionHeight="28dp"
        app:saturation="165"
        app:sensorHighlight="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingTop="18dp"
            android:paddingEnd="20dp"
            android:paddingBottom="12dp">

            <TextView
                android:id="@+id/tv_picker_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:shadowColor="#40000000"
                android:shadowDx="0"
                android:shadowDy="1"
                android:shadowRadius="3"
                android:textColor="#FFFFFFFF"
                android:textSize="19sp"
                android:textStyle="bold" />

            <LinearLayout
                android:id="@+id/picker_options"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:orientation="vertical" />
        </LinearLayout>
    </com.example.liquidglass.LiquidGlassView>
</FrameLayout>
